{
  "extends": "./src/tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  },
  "//comment": "Scoped to tests/types/ only — verifies type-only assertion files (currently the variance regression for IdevsGridEditControllerOptions). The vitest .test.ts files are transpiled by esbuild and not type-checked here; integrating them would require addressing pre-existing test-file strict-type issues outside this PR.",
  "include": [
    "src/**/*",
    "tests/types/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
